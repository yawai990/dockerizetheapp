import { Cardo, Noto_Sans } from "next/font/google";

export const title = Cardo({
  weight: "400",
  subsets: ["latin"],
});

export const body = Noto_Sans({
  weight: "400",
  subsets: ["latin"],
});
